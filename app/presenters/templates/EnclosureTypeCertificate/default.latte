{block content}
<div class="container">
    <h1 n:block="title" class="text-xs-center" >Přehled certifikátů pro výběhy</h1>
    <a n:href="EnclosureTypeCertificate:create" class="col-xs-2 offset-xs-8 btn btn-primary">Zadat nový</a>
    <div class="col-xs-8 offset-xs-2 card bg-faded">
        <table class="table">
            <thead>
            <tr>
                <th>#</th>
                <th>Název</th>
                <th>Typ výběhu</th>
                <th>Uživatel</th>
                <th>Expirace</th>
            </tr>
            </thead>
            {foreach $allCertificates as $certificate}
                <tr>
                    <th>{$certificate->getId()}</th>
                    <td><a n:href="EnclosureTypeCertificate:detail $certificate->getId()">{$certificate->getName()}</a></td>
                    <td>{$certificate->getEnclosureType()->getName()}</td>
                    <td>{$certificate->getUser()->getLastname()}</td>
                    <td>{$certificate->getEnd()|date:'%d.%m.%Y'}</td>
                </tr>
            {/foreach}
        </table>
    </div>
</div>
